#!/usr/bin/env bash
# List all configuration directories of me-<subcommand>

ETC_DIR="M4_ETC_DIR"

IFS=$'\n'
for dir in $(find "${ETC_DIR}" -maxdepth 1 -name 'me-*.d' -type d -printf "%f\n"); do
  printf "%s\n" "$(sed -nr 's@^me-(.*)\.d$@\1@p' <<< $dir)"
done

