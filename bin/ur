#!/usr/bin/env bash

log_iptables() {
  echo -e "${ME_ANSI_BLUE}$(date)${ME_ANSI_END}"
  iptables -vnL
  echo -e "\n\n"
}

pacman -Syu --noconfirm &&
log_iptables &>> /var/log/iptables.log &&
shutdown -r +2
